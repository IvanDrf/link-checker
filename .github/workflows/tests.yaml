name: Run tests

on:
    push:
        branches:
            - "**"

    pull_request:
        branches:
            - "**"

jobs:
    test:
        name: Run tests
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v

            - name: SetUp Go
              uses: actions/setup-go@v5
              with:
                  go-version: "1.25"

            - name: Run tests
              run: |
                  cd scripts
                  run: chmod +x run_tests.bash
                  run: ./run_tests.bash
